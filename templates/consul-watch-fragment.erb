template {
  source = "<%= @source_name %>"
  destination = "<%= @destination %>"
  command = "<%= @command %>"
<% if !@command_timeout.nil? && !@command_timeout.empty? -%>
  command_timeout = "<%= @command_timeout %>"
<% end -%>
}

