<%
options = {
  'facility' =>  scope.lookupvar('::consul_template::syslog_facility'),
}.delete_if { |k,v| v.nil? || v == :undef }

width = options.keys.map { |k| k.size }.max
%>
syslog {
  enabled = true

<% options.sort.each do |opt, val| -%>
  <%- val = "\"#{val}\"" if val.is_a?(String) -%>
  <%= opt.ljust(width) %> = <%= val %>
<% end -%>
}
